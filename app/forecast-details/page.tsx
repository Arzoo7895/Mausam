'use client'

import Link from 'next/link'
import { useMemo } from 'react'
import { ArrowLeft, CloudRain, Droplets, Eye, Gauge, Loader2, MapPin, RefreshCw, Sun, Thermometer, Wind } from 'lucide-react'
import { Area, AreaChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { useLocations } from '@/lib/weather/use-locations'
import { useWeather } from '@/lib/weather/use-weather'
import { formatTime, weatherCodeInfo } from '@/lib/weather/service'

function ChartCard({ title, children }: { title: string; children: React.ReactNode }) {
  return <section className="min-w-0 rounded-2xl border border-border bg-card p-5 shadow-sm"><h2 className="font-semibold">{title}</h2><div className="mt-5 h-64 min-h-64 w-full min-w-0">{children}</div></section>
}

export default function ForecastDetailsPage() {
  const { active } = useLocations()
  const { data, loading, error, refresh } = useWeather(active?.latitude, active?.longitude)
  const hourly = useMemo(() => data?.hourly.map((h) => ({ ...h, rain: h.precipProb })) ?? [], [data])
  const location = active ? `${active.name}${active.region ? `, ${active.region}` : ''}` : 'Selected location'

  return <main className="min-h-screen bg-background text-foreground"><header className="sticky top-0 z-20 border-b border-border/70 bg-background/90 backdrop-blur-xl"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-4 md:px-8"><div className="flex items-center gap-3"><Link href="/dashboard" aria-label="Back to dashboard" className="rounded-lg p-2 hover:bg-muted"><ArrowLeft size={18} /></Link><div><p className="text-xs uppercase tracking-[.2em] text-muted-foreground">Forecast details</p><p className="font-semibold">{location}</p></div></div><button onClick={refresh} aria-label="Refresh forecast" className="rounded-lg p-2 text-muted-foreground hover:bg-muted"><RefreshCw size={18} /></button></div></header><div className="mx-auto max-w-6xl px-4 py-8 md:px-8"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="flex items-center gap-2 text-sm text-muted-foreground"><MapPin size={14} className="text-primary" /> {location}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight md:text-4xl">Detailed forecast</h1><p className="mt-2 text-sm text-muted-foreground">Hourly signals and a seven-day outlook for planning with confidence.</p></div><Link href="/ai-recommendations" className="rounded-xl bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground">View AI recommendations</Link></div>{error && <div role="alert" className="mb-5 rounded-xl border border-destructive/30 bg-destructive/10 p-4 text-sm text-destructive">{error}</div>}{loading && !data ? <div className="flex min-h-96 items-center justify-center text-muted-foreground"><Loader2 className="mr-2 animate-spin" size={18} /> Loading forecast…</div> : data ? <><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Metric icon={Thermometer} label="Feels like" value={`${data.current.apparentC}°C`} detail={`${data.current.tempC}°C actual`} /><Metric icon={Droplets} label="Humidity" value={`${data.current.humidity}%`} detail="Relative humidity" /><Metric icon={Wind} label="Wind" value={`${data.current.windKmh} km/h`} detail="10m wind speed" /><Metric icon={Gauge} label="UV index" value={`${data.uvIndexMax}`} detail="Today maximum" /></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><ChartCard title="Next hours · temperature"><ResponsiveContainer width="100%" height="100%"><AreaChart data={hourly}><defs><linearGradient id="tempFill" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="var(--primary)" stopOpacity={0.3}/><stop offset="95%" stopColor="var(--primary)" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" stroke="var(--border)"/><XAxis dataKey="label" tickLine={false} axisLine={false} fontSize={11}/><YAxis unit="°" width={34} tickLine={false} axisLine={false} fontSize={11}/><Tooltip contentStyle={{ background: 'var(--card)', borderColor: 'var(--border)', borderRadius: 12 }}/><Area type="monotone" dataKey="tempC" name="Temperature" stroke="var(--primary)" fill="url(#tempFill)" strokeWidth={2}/></AreaChart></ResponsiveContainer></ChartCard><ChartCard title="Rain chance and wind"><ResponsiveContainer width="100%" height="100%"><LineChart data={hourly}><CartesianGrid strokeDasharray="3 3" stroke="var(--border)"/><XAxis dataKey="label" tickLine={false} axisLine={false} fontSize={11}/><YAxis yAxisId="rain" unit="%" width={34} tickLine={false} axisLine={false} fontSize={11}/><YAxis yAxisId="wind" orientation="right" unit=" km/h" width={48} tickLine={false} axisLine={false} fontSize={10}/><Tooltip contentStyle={{ background: 'var(--card)', borderColor: 'var(--border)', borderRadius: 12 }}/><Line yAxisId="rain" type="monotone" dataKey="rain" name="Rain chance" stroke="var(--chart-2)" strokeWidth={2} dot={false}/><Line yAxisId="wind" type="monotone" dataKey="windKmh" name="Wind" stroke="var(--chart-3)" strokeWidth={2} dot={false}/></LineChart></ResponsiveContainer></ChartCard></div><section className="mt-5 rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Seven-day outlook</h2><p className="mt-1 text-xs text-muted-foreground">Temperature range, rain chance, and UV exposure</p></div><Sun className="text-primary" size={20}/></div><div className="mt-5 grid grid-cols-2 gap-2 sm:grid-cols-4 lg:grid-cols-7">{data.daily.map((day) => <div key={day.date} className="rounded-xl bg-muted/60 p-3"><p className="text-xs font-medium">{day.label}</p><div className="my-3 text-primary"><WeatherIcon code={day.code}/></div><p className="text-lg font-semibold">{day.maxC}° <span className="text-sm font-normal text-muted-foreground">{day.minC}°</span></p><p className="mt-2 text-xs text-muted-foreground">{day.precipProb}% rain</p><p className="mt-1 text-xs text-muted-foreground">UV {day.uvIndex}</p></div>)}</div></section><div className="mt-5 grid gap-4 sm:grid-cols-3"><Metric icon={Eye} label="Visibility" value={`${data.current.visibilityKm} km`} detail="Current conditions" /><Metric icon={CloudRain} label="Air quality" value={data.airQuality.usAqi ? `AQI ${data.airQuality.usAqi}` : 'Unavailable'} detail={data.airQuality.pm25 ? `PM2.5 ${data.airQuality.pm25}` : 'No air-quality reading'} /><Metric icon={Sun} label="Sun cycle" value={formatTime(data.sunrise)} detail={`Sunset ${formatTime(data.sunset)}`} /></div></> : <p className="py-24 text-center text-sm text-muted-foreground">No forecast available.</p>}</div></main>
}

function Metric({ icon: Icon, label, value, detail }: { icon: typeof Thermometer; label: string; value: string; detail: string }) { return <div className="rounded-2xl border border-border bg-card p-4"><Icon size={18} className="text-primary"/><p className="mt-4 text-xs text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></div> }
function WeatherIcon({ code }: { code: number }) { const icon = weatherCodeInfo(code).icon; return icon === 'rain' ? <CloudRain size={20}/> : icon === 'sun' ? <Sun size={20}/> : <CloudRain size={20}/> }
