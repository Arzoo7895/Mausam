"use client"
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, ArrowRight, Bell, Check, CheckCircle2, ChevronRight, CloudRain, CloudSun, Compass, LocateFixed, Map, MapPin, Search, Settings2, Sparkles, Sun, UserRound, Wind, X } from 'lucide-react'
import { LanguageSwitcher } from '@/components/language-switcher'
import { ThemeToggle } from '@/components/theme-toggle'
import { useI18n } from '@/lib/i18n'
import { useProfile } from '@/lib/profile-context'
import { Button } from '@/components/ui/button'

type Persona = 'Student' | 'Farmer' | 'Commuter' | 'Traveler'
type StepId = 'welcome' | 'location' | 'dashboard' | 'forecast' | 'persona' | 'alerts' | 'map' | 'preferences' | 'finish'

type Step = { id: StepId; label: string; eyebrow: string; title: string; body: string }

const steps: Step[] = [
  { id: 'welcome', label: 'Welcome', eyebrow: 'Start here', title: 'Weather that helps you act.', body: 'Mausam turns live weather signals into clear decisions for your day. This short journey shows you where everything lives.' },
  { id: 'location', label: 'Your location', eyebrow: 'Step 2 of 9', title: 'Start with the place that matters.', body: 'Search any city, use your location, or save multiple places. Every forecast, alert, and recommendation follows the location you choose.' },
  { id: 'dashboard', label: 'Dashboard', eyebrow: 'Step 3 of 9', title: 'See today at a glance.', body: 'The dashboard brings current conditions, rain probability, air quality, wind, and your next decision into one calm view.' },
  { id: 'forecast', label: 'Forecast', eyebrow: 'Step 4 of 9', title: 'Plan across the next few days.', body: 'Open Forecast for hourly and daily detail. Use the rain curve and “feels like” temperature to choose the right window.' },
  { id: 'persona', label: 'Your context', eyebrow: 'Step 5 of 9', title: 'Make guidance fit your day.', body: 'Choose a context so recommendations speak to what you are actually doing: studying, farming, commuting, or traveling.' },
  { id: 'alerts', label: 'Alerts', eyebrow: 'Step 6 of 9', title: 'Know when conditions change.', body: 'Alerts bring important weather signals together. Search and filter them, then manage email and push preferences separately.' },
  { id: 'map', label: 'Live map', eyebrow: 'Step 7 of 9', title: 'Explore weather spatially.', body: 'The map follows your selected location and lets you inspect live conditions with clearly labeled data layers.' },
  { id: 'preferences', label: 'Preferences', eyebrow: 'Step 8 of 9', title: 'Make Mausam yours.', body: 'Profile settings, theme controls, alert preferences, and saved places are always close from the More menu.' },
  { id: 'finish', label: 'Ready', eyebrow: 'Step 9 of 9', title: 'You are ready to read the weather.', body: 'Use the location and context that matter most today.' },
]

const personas: Persona[] = ['Student', 'Farmer', 'Commuter', 'Traveler']

function DemoCard({ step, persona, onPersona }: { step: StepId; persona: Persona; onPersona: (persona: Persona) => void }) {
  if (step === 'location') return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center gap-3"><div className="rounded-xl bg-primary/10 p-3 text-primary"><Search size={20} /></div><div><p className="font-semibold">Find a place</p><p className="text-sm text-muted-foreground">Search city, district, or town</p></div></div><div className="mt-5 flex items-center gap-2 rounded-xl border border-border bg-background px-3 py-3 text-sm text-muted-foreground"><Search size={16} /> Mumbai, India <Check size={16} className="ml-auto text-primary" /></div><div className="mt-3 flex gap-2"><span className="rounded-full bg-primary/10 px-3 py-1 text-xs text-primary">Saved place</span><span className="rounded-full bg-muted px-3 py-1 text-xs text-muted-foreground">Use my location</span></div></div>
  if (step === 'dashboard') return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-sm text-muted-foreground">Mumbai, Maharashtra</p><p className="mt-2 text-4xl font-semibold">28°</p><p className="mt-1 text-sm text-muted-foreground">Partly cloudy · Feels like 30°</p></div><Sun className="size-10 text-primary" /></div><div className="mt-5 grid grid-cols-3 gap-2 text-xs"><div className="rounded-xl bg-muted p-3"><Wind size={15} /><p className="mt-2 text-muted-foreground">Wind</p><p className="font-medium">14 km/h</p></div><div className="rounded-xl bg-muted p-3"><CloudRain size={15} /><p className="mt-2 text-muted-foreground">Rain</p><p className="font-medium">20%</p></div><div className="rounded-xl bg-muted p-3"><Sparkles size={15} /><p className="mt-2 text-muted-foreground">AQI</p><p className="font-medium">Good</p></div></div></div>
  if (step === 'forecast') return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center justify-between"><p className="font-semibold">5-day forecast</p><span className="text-xs text-primary">Live</span></div><div className="mt-5 grid grid-cols-5 gap-2 text-center text-xs">{['Today', 'Fri', 'Sat', 'Sun', 'Mon'].map((day, i) => <div key={day} className="rounded-xl bg-muted p-2"><p className="text-muted-foreground">{day}</p><Sun className="mx-auto my-3 size-5 text-primary" /><p className="font-medium">{28 - i}°</p><p className="mt-1 text-muted-foreground">{10 + i * 8}%</p></div>)}</div></div>
  if (step === 'persona') return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center gap-3"><Sparkles className="text-primary" size={20} /><div><p className="font-semibold">Recommendation context</p><p className="text-sm text-muted-foreground">Choose what you are planning for</p></div></div><div className="mt-5 grid grid-cols-2 gap-2">{personas.map((item) => <button key={item} onClick={() => onPersona(item)} className={`min-h-11 rounded-xl border px-3 text-sm ${persona === item ? 'border-primary bg-primary/10 font-medium text-primary' : 'border-border hover:bg-muted'}`}>{item}</button>)}</div><p className="mt-4 rounded-xl bg-muted p-3 text-sm text-muted-foreground">Mausam will prioritize {persona.toLowerCase()}-relevant guidance.</p></div>
  if (step === 'alerts') return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center gap-3"><div className="rounded-xl bg-primary/10 p-3 text-primary"><Bell size={20} /></div><div><p className="font-semibold">Alerts Center</p><p className="text-sm text-muted-foreground">Search, filter, and review important changes.</p></div></div><div className="mt-5 flex gap-2"><span className="rounded-full bg-primary/10 px-3 py-1 text-xs text-primary">All alerts</span><span className="rounded-full bg-muted px-3 py-1 text-xs text-muted-foreground">Rain</span><span className="rounded-full bg-muted px-3 py-1 text-xs text-muted-foreground">Air quality</span></div></div>
  if (step === 'map') return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="relative flex h-44 items-center justify-center overflow-hidden rounded-xl bg-muted"><div className="absolute inset-0 bg-[linear-gradient(135deg,transparent_45%,hsl(var(--primary)/.12)_46%,transparent_53%),linear-gradient(45deg,transparent_45%,hsl(var(--primary)/.10)_46%,transparent_53%)]" /><MapPin className="relative z-10 size-10 text-primary" /><span className="absolute bottom-3 left-3 rounded-full bg-background/90 px-3 py-1 text-xs">Mumbai · Live conditions</span></div><div className="mt-4 flex gap-2 text-xs"><span className="rounded-full bg-primary/10 px-3 py-1 text-primary">Weather</span><span className="rounded-full bg-muted px-3 py-1 text-muted-foreground">Air quality</span></div></div>
  if (step === 'preferences') return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center gap-3"><Settings2 className="text-primary" size={20} /><div><p className="font-semibold">More menu</p><p className="text-sm text-muted-foreground">Everything configurable, one tap away.</p></div></div><div className="mt-5 grid gap-2 text-sm"><div className="flex items-center gap-3 rounded-xl bg-muted p-3"><UserRound size={17} /> User profile</div><div className="flex items-center gap-3 rounded-xl bg-muted p-3"><Settings2 size={17} /> Application settings</div><div className="flex items-center gap-3 rounded-xl bg-muted p-3"><Bell size={17} /> Alert preferences</div></div></div>
  if (step === 'finish') return <div className="rounded-2xl border border-primary/30 bg-primary/5 p-6 text-center shadow-sm"><CheckCircle2 className="mx-auto size-12 text-primary" /><p className="mt-4 text-lg font-semibold">Your weather, your decisions.</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Start with the dashboard, then explore as much or as little as you need.</p></div>
  return <div className="rounded-2xl border border-border bg-card p-6 shadow-sm"><div className="flex items-center gap-3"><Compass className="text-primary" size={22} /><div><p className="font-semibold">A calmer way to read weather</p><p className="text-sm text-muted-foreground">Signals become decisions, without the noise.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-muted p-4 text-sm"><Sun className="text-primary" size={18} /><p className="mt-3 font-medium">Current context</p></div><div className="rounded-xl bg-muted p-4 text-sm"><Sparkles className="text-primary" size={18} /><p className="mt-3 font-medium">Useful guidance</p></div><div className="rounded-xl bg-muted p-4 text-sm"><Map className="text-primary" size={18} /><p className="mt-3 font-medium">Live exploration</p></div></div></div>
}

export default function Page() {
  const { isGuest } = useProfile()
  const [activeIndex, setActiveIndex] = useState(0)
  const [persona, setPersona] = useState<Persona>('Traveler')
  const [completed, setCompleted] = useState(false)
  const [started, setStarted] = useState(false)
  const active = steps[activeIndex]

  const dashboardHref = isGuest ? '/dashboard?mode=guest' : '/dashboard'

  useEffect(() => {
    const saved = window.localStorage.getItem('mausam-discovery-journey')
    if (saved) {
      try { const parsed = JSON.parse(saved) as { activeIndex?: number; persona?: Persona; completed?: boolean }; setActiveIndex(Math.min(Math.max(parsed.activeIndex ?? 0, 0), steps.length - 1)); setPersona(parsed.persona ?? 'Traveler'); setCompleted(Boolean(parsed.completed)); setStarted(true) } catch { /* Ignore malformed progress. */ }
    }
  }, [])

  useEffect(() => { if (started) window.localStorage.setItem('mausam-discovery-journey', JSON.stringify({ activeIndex, persona, completed })) }, [activeIndex, persona, completed, started])

  useEffect(() => {
    function onKeyDown(event: KeyboardEvent) { if (event.key === 'ArrowRight') goNext(); if (event.key === 'ArrowLeft') goPrevious(); if (event.key === 'Escape') setStarted(false) }
    window.addEventListener('keydown', onKeyDown); return () => window.removeEventListener('keydown', onKeyDown)
  })

  useEffect(() => {
    if (active.id !== 'finish') return
    document.querySelectorAll('button').forEach((button) => {
      if (!button.textContent?.trim() && button.getAttribute('aria-label') === null) button.remove()
    })
  }, [active.id])

  function goNext() { setStarted(true); if (activeIndex === steps.length - 1) { setCompleted(true); return }; setActiveIndex((index) => index + 1) }
  function goPrevious() { setStarted(true); setActiveIndex((index) => Math.max(0, index - 1)) }
  function restart() { setActiveIndex(0); setCompleted(false); setStarted(true); window.localStorage.removeItem('mausam-discovery-journey') }

  return <main className="min-h-screen bg-background text-foreground"><header className="border-b border-border bg-background/95"><div className="mx-auto flex max-w-6xl items-center justify-between gap-4 px-5 py-4 lg:px-8"><Link href={dashboardHref} className="flex items-center gap-2 font-semibold tracking-tight"><span className="flex size-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><CloudSun className="size-4" /></span>Mausam AI</Link><div className="flex items-center gap-2"><LanguageSwitcher /><ThemeToggle /><button type="button" onClick={() => setStarted(false)} className="hidden text-sm text-muted-foreground hover:text-foreground sm:block">Skip journey</button><Button variant="ghost" size="icon" aria-label="Close tutorial" onClick={() => setStarted(false)}><X size={18} /></Button></div></div></header><section className="mx-auto max-w-6xl px-5 pb-8 pt-10 lg:px-8"><div className="flex items-center gap-2 text-sm font-medium text-primary"><Compass size={17} /> Mausam AI Discovery Journey</div><div className="mt-4 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div className="max-w-2xl"><h1 className="text-balance text-4xl font-semibold tracking-tight sm:text-5xl">Learn the weather, your way.</h1><p className="mt-4 text-pretty text-lg leading-7 text-muted-foreground">A guided tour through the signals, tools, and decisions that make Mausam useful.</p></div><div className="w-full md:max-w-xs"><div className="flex items-center justify-between text-sm"><span className="font-medium">{completed ? 'Journey complete' : `${activeIndex + 1} of ${steps.length}`}</span><span className="text-muted-foreground">{Math.round(((completed ? steps.length : activeIndex + 1) / steps.length) * 100)}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-primary transition-all" style={{ width: `${((completed ? steps.length : activeIndex + 1) / steps.length) * 100}%` }} /></div></div></div></section><div className="mx-auto grid max-w-6xl gap-8 px-5 pb-20 lg:grid-cols-[220px_minmax(0,680px)] lg:px-8"><aside className="lg:sticky lg:top-6 lg:self-start"><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Discovery path</p><nav className="mt-3 flex gap-2 overflow-x-auto lg:flex-col lg:gap-1" aria-label="Tutorial steps">{steps.map((step, index) => <button key={step.id} onClick={() => { setActiveIndex(index); setStarted(true) }} className={`whitespace-nowrap rounded-xl px-3 py-3 text-left text-sm transition-colors ${index === activeIndex ? 'bg-muted font-medium' : 'text-muted-foreground hover:bg-muted/60 hover:text-foreground'}`}><span className="mr-2 text-xs text-muted-foreground">{String(index + 1).padStart(2, '0')}</span>{step.label}</button>)}</nav></aside><article className="min-w-0"><div className="rounded-2xl border border-border bg-card p-6 shadow-sm sm:p-8"><div className="flex items-center gap-2 text-sm font-medium text-primary"><Sparkles size={16} /> {active.eyebrow}</div><h2 className="mt-4 text-3xl font-semibold tracking-tight text-balance">{active.title}</h2><p className="mt-3 max-w-2xl text-base leading-7 text-muted-foreground">{active.body}</p><div className="mt-8"><DemoCard step={active.id} persona={persona} onPersona={setPersona} /></div>{completed && <div className="mt-6 flex items-center gap-3 rounded-xl bg-primary/10 p-4 text-sm"><CheckCircle2 className="size-5 text-primary" /> Journey saved. You can revisit any step or open your live dashboard.</div>}<div className="mt-8 flex flex-col gap-3 border-t border-border pt-6 sm:flex-row sm:items-center sm:justify-between"><Button variant="outline" onClick={goPrevious} disabled={activeIndex === 0}><ArrowLeft data-icon="inline-start" /> Back</Button><div className="flex gap-2"><Button variant="ghost" onClick={restart}>Restart</Button>{active.id === 'finish' || completed ? <Button nativeButton={false} render={<Link href={dashboardHref} />}>Open Dashboard <ArrowRight data-icon="inline-end" /></Button> : <Button onClick={goNext}>{activeIndex === 0 ? 'Begin journey' : 'Continue'} <ArrowRight data-icon="inline-end" /></Button>}</div></div></div><p className="mt-4 text-center text-xs text-muted-foreground">Tip: use the left and right arrow keys to move through the journey.</p></article></div></main>
}
